{
  "name": "yan-liang-portfolio",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:drafts": "node scripts/build.mjs --drafts",
    "check": "node scripts/check.mjs"
  },
  "devDependencies": {
    "@fontsource-variable/instrument-sans": "^5.3.0",
    "@fontsource-variable/newsreader": "^5.3.0",
    "gray-matter": "^4.0.3",
    "markdown-it": "^15.0.0"
  }
}
